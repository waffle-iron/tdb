/*StashedTech.methods = {};
StashedTech.methods.add = new ValidatedMethod({
  name: 'StashedTech.methods.add',
  validate: Schemas.StashedTech.validator(),
  run(doc) {
    return StashedTech.insert(doc);
  }
});
*/