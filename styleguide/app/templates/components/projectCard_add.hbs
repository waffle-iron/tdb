<div class="ibox card-box cards-item">
  <div class="ibox-content no-padding card-label">
    <img src="{{src}}" class="img-responsive" />
    <div class="card-description">
      <h5 class="card-name"><span class="card-id">{{evID}}</span> {{name}}</h5>
      <p>{{description}}</p>
    </div>
  </div>
</div>
</div>
